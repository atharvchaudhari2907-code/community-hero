NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/v1
NEXT_PUBLIC_APP_ENV=development

# The live issue map uses Leaflet + OpenStreetMap tiles - no API key needed.
# This is only needed if you later swap in Google Maps for geocoding/places.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
